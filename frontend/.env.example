# Backend API URL
VITE_API_URL=http://localhost:8000

# Bearer token for API authentication (generate with: openssl rand -hex 32)
# Must match the API_BEARER_TOKEN configured in the backend
VITE_CHAT_API_TOKEN=your-64-character-hex-token-here
